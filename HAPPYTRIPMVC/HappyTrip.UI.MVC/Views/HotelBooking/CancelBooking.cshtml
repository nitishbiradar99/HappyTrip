@model HappyTrip.Models.CancelBookingHotel

@{
    ViewBag.Title = "CancelBooking";
}

<h2>CancelBooking</h2>

@using (Html.BeginForm("CancelBookingDetails", "HotelBooking", FormMethod.Post))
{
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CancelBooking</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.BookingIdNo)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.BookingIdNo)
            @Html.ValidationMessageFor(model => model.BookingIdNo)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EmailId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EmailId)
            @Html.ValidationMessageFor(model => model.EmailId)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
