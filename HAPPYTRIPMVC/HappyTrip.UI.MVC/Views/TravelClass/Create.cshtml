@model HappyTrip.Models.TravelClass

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TravelClass</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.TravelClassName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TravelClassName)
            @Html.ValidationMessageFor(model => model.TravelClassName)
        </div>

        <p>
            <input type="submit" value="Create" class="button-secondary"/>
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
